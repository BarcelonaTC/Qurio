<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/container_play_button"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/orange_variant"
    android:paddingStart="24dp"
    android:paddingEnd="24dp">

    <com.barcelona.qurio.customView.PlayButtonView
        android:id="@+id/play_shape"
        android:layout_width="166dp"
        android:layout_height="44dp"
        android:background="@android:color/transparent"
        android:backgroundTint="@color/primary"
        android:clickable="true"
        android:focusable="true"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/textView"
        style="@style/Label.Medium"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/play_now"
        android:textColor="@color/on_primary"
        app:layout_constraintBottom_toBottomOf="@+id/play_shape"
        app:layout_constraintEnd_toEndOf="@+id/play_shape"
        app:layout_constraintStart_toStartOf="@+id/play_shape"
        app:layout_constraintTop_toTopOf="@+id/play_shape" />

    <ImageView
        android:id="@+id/imageView2"
        android:layout_width="6dp"
        android:layout_height="6dp"
        android:layout_marginStart="5dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="30dp"
        android:contentDescription="@string/star_icon"
        app:layout_constraintBottom_toBottomOf="@+id/play_shape"
        app:layout_constraintStart_toEndOf="@+id/textView"
        app:layout_constraintTop_toTopOf="@+id/play_shape"
        app:srcCompat="@drawable/star"
        app:tint="@color/on_primary" />

    <ImageView
        android:id="@+id/imageView3"
        android:layout_width="5dp"
        android:layout_height="5dp"
        android:layout_marginTop="17dp"
        android:layout_marginEnd="4dp"
        android:layout_marginBottom="22dp"
        android:contentDescription="@string/star_icon"
        app:layout_constraintBottom_toBottomOf="@+id/play_shape"
        app:layout_constraintEnd_toStartOf="@+id/textView"
        app:layout_constraintTop_toTopOf="@+id/play_shape"
        app:srcCompat="@drawable/star"
        app:tint="@color/on_primary" />

</androidx.constraintlayout.widget.ConstraintLayout>